# Install plugins if there are plugins that have not been installed.
if zplug check --verbose; then
  return
fi

printf "Install? [y/N]: "
if read -q; then
  echo
  # https://github.com/zplug/zplug/issues/243
  # ZPLUG_CLONE_DEPTH=1
  zplug install
fi

# vim:set filetype=sh:
