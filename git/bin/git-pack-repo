#!/usr/bin/env sh

git count-objects -v
printf "\nCompressing objects in repository...\n"
git repack -Ad      # Kill in-pack garbage.
git prune           # Kill loose garbage.
echo
git count-objects -v

