#!/usr/bin/env sh

if hash scutil 2> /dev/null && scutil --dns | grep --quiet --fixed-strings .therightstuff.de; then
  host=gateway-home.home.therightstuff.de
else
  host=home.therightstuff.de
fi

ssh "$host" -t sudo \$HOME/bin/wake
