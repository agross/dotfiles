# shellcheck shell=bash

symlink "$topic/npmrc"

if [[ "$OSTYPE" == darwin* ]]; then
  formula nodenv

  ensure-homebrew-installed
  brew tap nodenv/nodenv
  brew trust nodenv/nodenv
  formula nodenv-package-rehash
fi

# vim: set ft=sh ts=2 sw=2 et:
